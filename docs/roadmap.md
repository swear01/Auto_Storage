- 2026-08-13 GitHub #92／PR #93：client recipe viewer 改為 optional EMI **或** JEI（EMI 優先）；JEI-only pack（如 ATM10）可載入。`v0.3.1` 已以 commit `1d0a120`（cold Craftable tag-matching 效能修正）發布，release run `31635247560` 全綠；canonical SHA `31d33960…`。ATM10 dogfood（#91）首次開機至 READY；world-prep 剝模板 `fml` 並 skip onboarding screens（見 notes.md）。
- 2026-08-06 GitHub #89：chance recipe EV → exact rational Core pending；Create milling/crushing/cutting dogfood；grid shows whole, tooltip may show pending.
- 2026-08-03 GitHub #67／PR #72 Create Enchantment Industry：Compat Kit Outcome C fail-closed（零 custom family）；present-mod module registers no stations/families；5 isolated GameTests plus descriptor-owned matrix coexistence. See [`create-enchantment-industry-compatibility.md`](create-enchantment-industry-compatibility.md)。
- **GitHub #64／PR #71 Mystical Agriculture**：Outcome B Seed Reprocessor only；scanner format 17 audit/contract、descriptor isolation、isolated digest `c1879fd1…1b8c`；rebased onto `a1582ad`（#87） with unchanged 9 MiB shared-index gate.
- 2026-08-02 GitHub #54／PR #60 rebased onto `4464493`（#78）：module-owned isolated digest + fixture evidence；no peer Theurgy/global digest edits；Gemini `exact()`/`keysWithoutRegistries` findings addressed.
- 2026-08-02 GitHub #77 evidence isolation：finish true module isolation after #75/#76 — isolated descriptor digests verified by isolated fixtures, matrix report records actual coexistence/unclaimed evidence without committed global digests, CI/release compatibility summary at `build/reports/compatibility-modules.md`.
- 2026-08-02 GitHub #57／PR #61 Productive Bees merged Outcome C：representative `1.21.1-13.13.5` format-16 audit rejects all 16 actual Recipe candidates and removes 55 legacy false positives; 8 isolated GameTests, matrix, latest-head GitHub Codex review and CI passed. PR #61 merged as `b5c587607ee1efb7a433e2bc68179c4fb1e406a8`. See [`productivebees-compatibility.md`](productivebees-compatibility.md)。
- 2026-08-02 GitHub #63／PR #69 Draconic Evolution merged Outcome C：representative `3.1.4.632` format-16 audit rejects concrete `FusionRecipe` and its target-defined `IFusionRecipe` interface, removes 11 legacy non-recipe false positives, and registers zero stations/resource kinds/families. PR #69 merged as `c790457ed00f4de43d34d38116ade15e7688bc07`. See [`draconicevolution-compatibility.md`](draconicevolution-compatibility.md)。
- 2026-08-02 GitHub #68 Create Aquatic Ambitions Compat Kit / merged Outcome C：representative `2.0.4` scanner-format-16 audit removes 20 legacy name-shaped false positives and rejects both actual Recipe candidates; present-mod module registers zero stations/families; 4 isolated GameTests plus descriptor-owned matrix coexistence lock outcome C. PR #70 merged as `034c96ebff3c21ab8524e6fad3aed97f2eff2d72`. See [`create-aquatic-ambitions-compatibility.md`](create-aquatic-ambitions-compatibility.md)。
- 2026-08-02 GitHub #65／PR #73 Advanced AE Compat Kit / merged Outcome C：representative `1.6.11-1.21.1` format-16 audit rejects the one actual Reaction Chamber recipe candidate and removes eleven legacy name-shaped non-recipe helpers. The present-mod module registers zero stations/families, 8 isolated GameTests lock outcome C, and descriptor-owned matrix metadata binds the 85 loaded `advanced_ae` recipes. PR #73 merged as `18b8c007e78177353d9925b6dad0a240c3138799`. See [`advanced-ae-compatibility.md`](advanced-ae-compatibility.md)。
- 2026-08-03 GitHub #53 Immersive Engineering：format-17 Outcome C（44 rejected）+ isolated digest/8 GameTests；PR #62 CI的52.990／68.603 ms證明舊「host contention only」判定不成立，stack-independent且已含typed plan的match改為listing-local直接重用，built-in／legacy output validation仍保留，且不新增retained recipe cache。Rebase onto `1102032` 後 21,022-recipe四槽exclusive matrix prepare 26.484 ms、3/3，50 ms／9 MiB／128 KiB gates unchanged；詳見[`immersiveengineering-compatibility.md`](immersiveengineering-compatibility.md)。
- 2026-08-02 GitHub #42／PR #46 Productive Metalworks merged Outcome C：format-16 audit只保留並拒絕5個concrete `Recipe` classes與shared `ICastingRecipe` interface，移除17個legacy non-recipe false positives；8 isolated GameTests、matrix、latest-head GitHub review及CI通過，squash merge為`9fc1aae99ed2b67a4ecd7c0fdbcbced858d5bada`。
- **Oritech pulverizer（GitHub #40）**：ATM10 representative `1.2.9` exact pulverizer family via Compat Kit; other shared-class RecipeContent types remain rejected until separate contracts exist.
- **Actually Additions Compat（GitHub #54）**：representative `1.3.26` format-16 audit accepts Crushing/Pressing/Fermenting and rejects 8 remaining families; isolated fixture `runActuallyadditionsGameTestServer` 14/14 verifies bucketless-fluid and fractional-primary/secondary fail-closed, guaranteed-secondary exact/rollback, and the module-owned recipe-inventory digest under #77/#78 evidence isolation.
- **0.3.0 Auto Storage Compat Kit（GitHub #34，complete / merged）**：development-only `scan/decide/diff/scaffold/verify/publish` CLI、strict schemas、reviewed repositories/evidence contracts、RED bundled/addon scaffolds、descriptor-driven fixture wiring與self-contained reproducible release archive已完成。首個dogfood以AE2 19.2.17 Inscriber的exact press/catalyst/AE→FE/work transaction及8個isolated GameTests驗證流程；scanner v7納入具名nested classes並排除multi-release aliases，AE2新增25個候選已明確審核。Bundled/external gates會驗resolved target jar SHA，external evidence不再remap，bundled descriptor保留reviewed repositories，complete contract會與獨立source audit的完整candidate set比對，fixture path在寫檔前驗證，generated addon runtime帶入required Patchouli與non-transitive reviewed target runtime dependencies，Maven repository precedence保持原順序，JVM synthetic flag與multi-release aliases會被排除，duplicate family IDs使用collision-free encoding，javap chance/randomness/generic-ingredient/capability-mutation descriptors可被辨識，所有target-derived TOML metadata正確escape、source checkout dirty state與malformed current cache會fail closed、GameTest evidence綁定annotated method body、target compile/runtime都保持non-transitive、family risk set不可刪除、explicit runtime groups不會fallback到Maven Central、matrix直接綁全部artifact verifiers、Process/Instant rates與runtime同規則、bundled GameTest task綁fixture camel case、free accepted family允許明確空costs、evidence只認method braces內文、AE2補真實missing-middle ingredient rollback、Java reserved package segment預先拒絕、candidate scanner-bucket binding、generator-owned verification-sensitive bytes、audited compile/runtime coordinate binding、enclosing Git worktree discovery、non-Git source rejection、destination/ancestor scaffold preflight、current-minor addon range、status-dependent contract schema、persisted scanner format、1 MiB metadata pre-decompression bound、candidate-to-source mapping、literal Groovy escaping、full external Gradle launcher/wrapper trust chain、non-vacuous report schema、symlink-safe GameTest world cleanup、comment-aware GameTest extraction、tracked-only source evidence、16 MiB class bound、post-scan artifact rehash、actual GameTest method-body/source-set binding、output-root上層symlink preflight、reused launcher mode repair、segment-boundary source mapping、risk-owner binding、integral descriptor test counts、single authoritative GameTest summary、bundled identifier collision preflight、explicit publish example/schema allowlists與version binding、TOML DEL escaping、empty-repository runtime-group isolation、dependency control-character rejection、normalized output-path preflight、complete-target/report-identity schemas與escaped text-block parsing、completed-contract verification schema、incremental private-bytecode reduction、modern Java randomness、exact Maven coordinates與comment-free GameTest evidence與platform-neutral live bounded `javap` streaming，任何artifact bytes變更都強制contract review。11,657-recipes的13-mod 10k/30k matrix、全部local CI gates及二十三輪Codex共88項finding均已修正。PR #38的latest head `a10317b7d4f8eae5b2dd32b7196fe36553da769e`已由手動觸發的GitHub Codex確認無actionable finding（comment `5140802142`）；PR CI run `30615286744`全綠，之後squash merge為`812196c991698797c42f9abc45a75d580ae23899`並自動關閉#34。Developer-only Wiki的`Addon Development`與`Compat Kit`已同步為Wiki commit `9b066cf08f2a1d37fa1fa9c33bd148f12064726c`，且未加入Home/sidebar。
- 2026-08-01 GitHub #75 declarative compatibility isolation：compat modules改由descriptor-owned matrix metadata與`runCompatFixtureGameTestServers`合流；矩陣assertion/inventory digests不再手改共享CI/Java清單。Merge queue不在本issue啟用。
- 2026-08-02 GitHub #55／PR #58 Railcraft Reborn merged：exact `1.2.10` format-16 audit reduces 52 legacy name-shaped families to 13 actual Recipe candidates and rejects all 13; present-mod registers zero stations/resource kinds/families. Eight isolated GameTests、three combined-matrix GameTests、latest-head GitHub Codex clean review及CI全綠，squash merge為`61aaf32f4faa263d8c3c6c6281f5ad81adc2e8b7`。
- 2026-08-01 GitHub #41 Theurgy：Calcination/Distillation/Liquefaction deterministic bridge已落地；88個audit候選明確reject；代表性Modrinth artifact與官方`release/v1.21.1-1.73.1` source對齊；isolated 9 GameTests、Compat Kit verify與共存matrix納入CI。
- 2026-07-30 0.3.0 breaking identity implementation與local gate completed：GitHub canonical repository、origin、active links與本機checkout目錄統一為`Auto_Storage`；mod ID/registry/resource/config/save/command/addon IDs統一為`auto_storage`，Java package統一為`com.swear.autostorage`，artifact統一為`auto_storage-0.3.0.jar`。0.2.x world、config、SavedData與addon identity不遷移；必須建立新世界，舊/新jar不得共存。Local `build`、EMI minimum/latest、全部isolated GameTests、10k/30k matrix、Python 308、datagen與jar identity全綠；發布仍以current main CI為準。
- 2026-07-30 listing metadata completed：Modrinth已補long description、MIT、categories/featured tags、source/issues與`0.2.0`的1.21.1/NeoForge metadata，並送交moderation；CurseForge已同步description/source、啟用comments且維持MIT/open distribution，file仍在review。Gallery/media等clean current fullscreen screenshot，不使用已知有缺陷的開發截圖。
- 2026-07-30 0.2.0-alpha published：`main` CI `30467153149`與release run `30468162688`均green；annotated `v0.2.0`已把同一jar成功送到GitHub、Modrinth與CurseForge。GitHub prerelease：<https://github.com/swear01/Auto_Storage/releases/tag/v0.2.0>；canonical SHA-256：`64cbe2705f2a1d20f83eb1bf848c1df7b74ffe9dab0e4fb958cfde498457b43c`。外部平台公開可見度仍由各自listing moderation生效。
- 2026-07-29 alpha publication preparation completed：公開metadata、512×512 icon、GitHub `publishing` environment、tag/publish/SHA/retry/roll-forward/secret-rotation流程已集中於[`docs/releasing.md`](releasing.md)並由Python contract守住；platform projects、credentials與`v0.2.0` publication已完成。
- 2026-07-29 Linux cold-Craftable release fix completed：NeoForge `DataComponentIngredient`現在用其完整item holder set進候選索引，但每個exact component variant仍走真實predicate；其他custom ingredient的non-exhaustive full scan不變。GitHub兩次56.481/73.663 ms failure的本機對應已降為三次針對性10k cold 6.842/7.769/11.922 ms、完整release gate 21.647 ms與30k 9.952 ms，83 outputs及heap gates不變。
- 2026-07-29 0.2.0-alpha release candidate：GitHub #27 audit、pre-push review、Codex final-review首輪六項及follow-up兩項修正與#28的10k/30k terminal gate皆完成。All aggregate互動／搜尋、Craftable clamp、Mekanism family label、Bus escrow-only recovery、雙層weak smithing component cache及detached typed-target exact conservation均有回歸。Catalog candidate直接使用reload snapshot holder並共用station availability，不保留會撞破heap gate的完整match。Current local為minimum/latest EMI compile、SelfTest 204908、base 405/405、recipe-addon 17/17、optional fixtures `47/14/3/7/7/11/10/11/9/13/9/4`、matrix 3/3、Python 297/297、build/runData green；10k matrix cold Craftable 11.753 ms、Storage interaction p95 5.648 ms、shared warm switch p95 0.162 ms、shared index 7,946,504 bytes、per-menu 114,391 bytes，原30k final數據仍為21.779 ms cold Craftable、0.820 ms cold Storage、22.447 ms interaction p95、0.189 ms shared switch p95。使用者明確接受目前fullscreen目視項目作為本alpha的release verdict；未來GUI變更仍要fresh F11驗收。
- 2026-07-29 terminal runtime-scale completed：runtime registry-driven `terminal-scale`會預載10k/30k exact variants、實際item、全部Processing/Instant descriptors與work reserves。Terminal不在Storage頁偷建Craftable；Core增量索引與shared cache讓首次Craftable construction本身低於50 ms。
- 2026-07-28 GitHub #28 completed：共用scrollbar區分grab-offset drag、track page/repeat與立即row wheel，無easing；recipe panel固定4px inset；recipes依可合成、adapter priority、完整ID排序並同步`n / craftable (total)`。自動gate與本版使用者目視結論均完成。
- 2026-07-28 superseded terminal viewport/Stations gate：0.1.62只完成立即row wheel、18列bank、All/Installed與移除外框；其pointer-centered scrollbar與舊效能數據已由GitHub #28取代。
- 2026-07-27 historical terminal consistency gate（scrollbar已由GitHub #28取代）：All aggregate精確顯示`Show: All`；搜尋裸operators、partial `@mod`、outside-click focus、initial-only Auto Focus由共用parser/router守住。Storage/Craftable改用server row-aligned scroll、120ms queued smooth wheel與framed track；Mekanism chemical以EMI exact glyph/amount顯示；Extended Crafting以81 positions/9 groups驗證。active Craftable不再重複保留visible cache，12-mod matrix per-menu為101,345 bytes且switch/prefetch/heap全過。完整自動gate為SelfTest 216834、base 398、全部optional/addon、matrix 3、Python 270、build/API/EMI/runData/diff全綠；0.1.57 exact-SHA已部署，`build/gui-runs/20260727-115305-crafting-fuel-page/`已到READY。下一步只需使用者完成current F11目視，通過前不宣稱GUI verified。
- 2026-07-27 terminal search + Extended Crafting：Crafting Terminal四頁搜尋共用同一focused key/char router與query parser；裸`@/#/$`等同空查詢並顯示全部，欄外點擊解除focus但保留query，各頁只更換候選scope。GitHub #25已關閉；Extended Crafting代表fixture以3個GameTests守住Ultimate Table gating、9×9/81-position且9個distinct groups的exact commit、one-short與destination rollback，並已接入CI/release與12-mod/10,986-recipe matrix；artifact版本只作CI證據，不釘玩家版本。下一次18-jar `crafting-fuel-page`世界會預載fluid/gas/FE/Mana recipes及9×9 stress recipe，使用者只需current F11目視。
- 2026-07-27 current naming/API correction：Resource selector的玩家名稱由`Station Work`改為`Processing`，各Processing數值hover改讀server-synced logical station family，不再把Crusher、Energizing Rod、Furnator等不同資源都叫同一名稱；內部`station_work` save/API key維持相容。Polymorphic descriptor API新增mandatory localized family `Component`並隨snapshot同步，protocol升至1.4。[MacFix 0.1.0](https://modrinth.com/mod/macfix)已用本機hash-pinned artifact加入18-jar macOS GUI support pack；它不成為玩家dependency，仍須current F11驗證delegate stub、display safety及graceful close。Final gate已通過：`build compileApiTestJava`、SelfTest 216822、base 398、addon 17、十一組optional fixtures、matrix 3、Python 261、`runData` no drift及`git diff --check`；GUI可見命名仍需下一次current F11使用者目視。
- 2026-07-26 GitHub #24 automated complete / current F11 pending：四頁寬版content bottom、空白top search、固定欄寬與RS2式scrollbar已統一。Direct Fuel card不再顯示`Direct`；station item hover保留原生物品tooltip，Processing數值hover只顯示value名稱與aggregate增加率，inventory旁status仍固定type capacity。GUI world contract守住`Integer.MAX_VALUE` Processing aggregate、count-1 prototype、exact tick、save/load與near-cap搬運。本次`build compileApiTestJava`、SelfTest 216821、base 398、Python 260、runData/diff全綠；0.1.53 build/deployed SHA-256同為`b9f0bf07f4a10f406b2d3b0e42a2fb1a6b1843bbea4bc3826681d913923e160b`，`build/gui-runs/20260726-233051-crafting-fuel-page/`已到READY，issue關閉前只剩使用者F11 visual verdict。
- 2026-07-26 current GUI correction automated complete / fullscreen pending：Stations的item/count/work改為同一geometry renderer並在頁面同步時重新定位互動slots；Transform壓縮上方amount strip、補空input提示並隱藏無意義的單頁箭頭。Core command-NBT載入會清理空的`onPlace`臨時record並立即attach指定storage，非空record不允許靜默替換。GREEN為SelfTest 214653、base 398、Python 258、build/runData/diff check；0.1.50 exact-SHA已部署，剩使用者F11 visual verdict。
- 2026-07-26 GitHub #23 second-pass automated complete / fullscreen pending：Transform輸入與amount strip移到上方，cards填滿剩餘高度並內聯Direct或station/work來源，不再畫重複的selected preview。Stations的Processing取得較大三欄區，installed aggregate疊在item、累積work水平並列；Instant改為無work的compact icon grid，兩區仍依高度自適應並保留paging/search。該revision資源selector仍使用舊名Station Work（已由2026-07-27的Processing命名取代），Energy保留FE與generated consumable reserves；`crafting-fuel-page`預載Powah Furnator讓Coal同時檢查Fuel/FE uses。Final automated gate為`build`、EMI minimum/latest compile、SelfTest 214653、base 396、addon 17、optional fixtures 47/14/3/7/7/11/10/11/9/13/9、matrix 3、Python 257、`runData` no drift與diff check；0.1.49 exact-SHA已部署，新版F11 visual verdict完成前不得關閉issue。
- 2026-07-25 historical 0.1.47 release gate（scale與門檻已由GitHub #28取代）：0.1.47的full-support Craftable benchmark現在固定10,890 recipes、512 stored types、655 outputs。三個獨立process的first switch為0.448–0.506 ms、switch p95為0.257–0.321 ms、warm p95為0.188–0.338 ms；steady prefetch p95為49.103–63.392 ms，shared cache低於7.64 MB、每menu低於102.5 KB。Hard gates分別為switch <50 ms、prefetch p95 <100 ms、shared <8 MiB、per-menu <128 KiB；cold/JIT preload另行報告，不冒充切頁值。#21效能已具自動化實證；下一步只跑一次`crafting-fuel-page` current F11 batched visual gate，READY或GameTest仍不能代替使用者GUI目視。
- 2026-07-25 #13–#20 automated acceptance complete / visual verdict pending：兩批optional-mod自動化已完成並補上7個isolated boundary regressions、2個all-mod coexistence GameTests及1個Craftable效能/heap GameTest。`runCompatibilityMatrixGameTestServer`會同時載入Mekanism、Botania、Iron Furnaces、Farmer's Delight、Modern Industrialization、Ars Nouveau、EvilCraft、Powah、Industrial Foregoing、Create、PneumaticCraft，守住registry/resource-kind/family共存與PneumaticCraft fail-closed；CI/release共14個序列GameTest runs。#13/#14/#15/#17/#18/#19仍待使用者current F11 fullscreen目視；#16/#20不需要combined GUI acceptance。
- 2026-07-24 GitHub #19 automated complete / batched fullscreen pending：Create 6.0.10透過optional isolated bridge支援Milling、Crushing、Cutting、Filling與Emptying。Exact item/fluid、crafting remainder、deterministic multi-output與recipe/fixed station work走同一simulate-then-commit transaction；13個隔離GameTests守住五family、station reload、duplicate/distinct-output handling、shortage/overflow rollback及chance/zero-time/unsupported-family拒絕。Pressing、Mixing/Compacting、Deploying、fan/world catalyst、Mechanical Crafting、Sequenced Assembly與RPM/stress模擬仍fail closed；CI artifact只作代表性證據，不釘玩家版本。完整邊界見`docs/create-compatibility.md`；GUI留待整批F11驗收。
- 2026-07-24 GitHub #20 source/API audit complete / fail-closed CI slice：PneumaticCraft 8.2.22的`IAirHandler`沒有simulate或accepted amount、允許負Air，pressure又由mutable volume派生；Pressure Chamber、Thermo Plant、Fluid Mixer、Assembly、Refinery、Heat Frame與Explosion另依賴retained pressure/heat/multiblock/world state。Auto Storage因此不註冊`pneumaticcraft:air`、Bus/container bridge、station或recipe family，也不把Air冒充FE/Fuel/Fluid。9個present-mod GameTests鎖住real recipes、全受審type掃描與Air semantics仍fail closed；代表artifact不釘玩家版本。完整判定見`docs/pneumaticcraft-compatibility.md`。
- 2026-07-24 GitHub #18 automated complete / batched fullscreen pending：Industrial Foregoing 1.21-3.6.39透過optional isolated bridge支援Dissolution Chamber、Material Stonework Factory與Crusher。Exact item/fluid、live-config FE、recipe/station work、catalyst、primary/byproduct走同一simulate-then-commit transaction；9個隔離GameTests守住rates、persistence、batch、shortage/overflow rollback及partial-threshold/ambiguous/world-state拒絕。Laser Drill、Fluid Extractor、client-synthetic recipe與custom output callback仍fail closed；CI artifact只作代表性證據，不釘玩家版本。完整邊界見`docs/industrial-foregoing-compatibility.md`；依使用者要求不單獨啟動Prism，待#14–#20完成後整批驗收。
- 2026-07-24 GitHub #14–#20 compatibility batch：#14–#19 automated bridges/fixtures完成；combined EMI/TMRV client pack因TMRV 0.9.0與EvilCraft 1.2.91 Spirit Furnace packet的真實FATAL而排除#16 EvilCraft/Cyclops Core，#16仍保留10個隔離GameTests。#20完成source/API audit並以9個present-mod tests守住零unsafe production contract，故也不加入GUI pack。其餘#14–#15/#17–#19已由15-jar Prism pack與scenario-owned預載Core lab部署到0.1.43，current run到READY；可見契約仍集中由使用者完成一次Minecraft F11驗收，在該verdict前不得把READY或GameTest宣稱為GUI通過。
- 2026-07-24 GitHub #13 Botania/Mana first safe slice automated complete：官方BlameJared Maven的NeoForge 1.21.1 `455-SNAPSHOT` build31已驅動隔離present-mod fixture；因snapshot coordinate可移動，CI先驗resolved jar SHA-256再編譯/執行，且只作代表性證據、不釘玩家依賴。已完成conditional `botania:mana` long ledger/Energy view、finite local Mana Tablet held transfer與Mana Infusion、Runic Altar、Terrestrial Agglomeration、Petal Apothecary、Elven Trade五個matching instant station families；13個GameTests守住mixed transaction、retained catalysts、remainders、water、multi-output、overflow與fail-closed。Botanical Brewery因output依runtime container且static result為空，Pure Daisy/Orechid因world/random，ManaReceiver因無simulate，仍明確排除。完整共享worktree automated gate已通過；Botania、Curios及Mana預載已加入目前15-jar Prism support pack與batched GUI world，GUI verdict仍保留在#13。
# Roadmap

- **GitHub #86 declarative exact runtime artifact transforms（local complete）**：bundled descriptor可安全移除第三方artifact內會被NeoForge提前classload的unrelated test-only entries；pristine artifact仍守住audit／compile／SHA，isolated/matrix只載入共享的deterministic transformed output。相同dependency contract跨module deduplicate，衝突或duplicate runtime fail closed；Integrated Dynamics／Integrated Crafting的實際descriptor接線由後續module integration完成。

- **0.3.0 modular compatibility SDK（GitHub #32，complete / merged）**：十一個built-in integrations已改為metadata-gated、target-isolated modules，並與第三方addon共用one-call facade、focused registries、bounded lifecycle hooks及compile-only API artifacts。Local完整gate、四項pre-push Major修正、GitHub Wiki `Addon Development`同步、PR #35 merge/main CI與issue closure均完成；每個module以自己的descriptor驅動build/runtime aggregation。Review-only PR #36以`297b7a2b74..0d26517a88`精確重建原feature diff，並由`github-pr-review-loop`選定的首個available provider GitHub Codex回覆無major finding；Cursor的disabled routing回覆只作availability evidence，不是第二個required clean review。0.3.0後續依`docs/releasing.md`的一般tag gate進行。

- **0.3.0 breaking identity release（GitHub #31，complete / published）**：統一`auto_storage` mod/registry/persistence identity、`com.swear.autostorage` Java package與`auto_storage-0.3.0.jar`；不提供0.2.x migration或legacy alias。Annotated tag `v0.3.0` on `54eb516` passed release run `31121735740`；GitHub prerelease <https://github.com/swear01/Auto_Storage/releases/tag/v0.3.0>；GitHub/Modrinth/CurseForge player jar SHA-256 `56883bbc33d3fe8dca4e5df5eee30b15f4af449a710fe737422daa01786e18b1`一致，GitHub Release另附API artifacts與Compat Kit archive。

- 2026-07-24 GitHub #17 automated complete / batched fullscreen pending：Powah Energizing支援1–6個simple exact item inputs、exact output count、scaled Core FE與相同station work；Starter–Nitro rod variant速率直接取loaded transfer config。9個isolated GameTests守住官方Energized Steel、六輸入batch、NeoForge Energy capability、Terminal/reload persistence、FE/work shortage與overflow rollback。Physical 20-tick warm-up、world link、generators/reactors與dynamic recipes明確排除；CI artifact只作代表性證據，不釘玩家版本。完整邊界見`docs/powah-compatibility.md`；依使用者要求不單獨啟動Prism，待#14–#20完成後整批驗收。
- 2026-07-24 GitHub #16 automated complete / batched fullscreen pending：EvilCraft Blood沿用shared fluid ledger；Blood Infuser支援exact item/Blood/Promise tier/duration/output atomic plans，10個isolated GameTests與CI/release gate已完成。XP、tag output、Purifier與live Promise modifiers仍fail closed；詳見`docs/evilcraft-compatibility.md`。
- 2026-07-24 GitHub #15 automated complete / batched fullscreen pending：Ars Nouveau Source已接入同一typed ledger與Source Jar capability；Imbuement Chamber及Enchanting Apparatus使用exact Source/item/catalyst/remainder/component transaction，11個isolated GameTests及CI/release gate已完成。Dynamic/world/超過九輸入配方仍fail closed；詳見`docs/ars-nouveau-compatibility.md`。
- 2026-07-24 GitHub #14 automated complete / batched fullscreen pending：Modern Industrialization 2.5.4的14個direct machine families已有optional reflective bridge、provider-derived Bronze/Steel/LV 2/4/8 rates、typed item/fluid/EU/catalyst/multi-output atomic plans與7個isolated GameTests；CI artifact只作代表性證據，不釘玩家版本。Dynamic furnace/stonecutting/compostable proxies、Forge Hammer、RNG、live warm/upgrade semantics仍明確fail closed。完整支持表見`docs/modern-industrialization-compatibility.md`；GUI不單獨啟動，待#14–#20全部完成後整批驗收。
- 2026-07-24 GitHub #11/#12 automated complete / fullscreen pending：Mekanism九個factory-backed recipe families已支援basic machine及3/5/7/9-process factories的exact速率，另支援Pressurized Reaction、兩種安全單向Rotary shape及十一種deterministic single-block fluid/chemical machine families。Timed workstation可累積、持久化及安全取回到`Integer.MAX_VALUE`；130只是GUI lab樣本，不是上限。Fuel三欄保留滾輪與前後頁控制、跨子分頁shift-click exact routing及timed station名稱/總work-per-tick tooltip；右下Fuel search啟用後把reserves與三類descriptor合併成單一paged grid，並比對所有concrete variants。完整automated gate為SelfTest 264525、base 380、addon 17、Mekanism 47、Botania 12、Iron Furnaces 3、Farmer's Delight 7、Python 236、build/EMI minimum+latest compile/runData no-drift/diff全綠；Fuel/Mekanism畫面只剩current F11 fullscreen目視，不可由自動測試代替。
- 2026-07-23 Search control correction：Auto Focus不再冒充Search Sync值；它是獨立、預設On的client-global boolean，開啟終端時只提供初始搜尋focus，點擊欄外會解除focus但保留query/filter。Search Sync改為Off/EMI/EMI two-way。Server search新增一次compile、Core metadata cache與Craftable preview前置過濾，client debounce由8 ticks縮成2 ticks。完整automated gate已綠並部署0.1.41；current fullscreen handoff已到READY，使用者目視結論仍待回報，詳見`docs/plan.md`最上方。
- 2026-07-23 Search/station UX correction（歷史中間狀態；搜尋契約已由上方Auto Focus修正取代）：recipe station badge保留installed-first，但按EMI/JEI的real-time 1000ms速度輪播。搜尋欄直接解析名稱、`@mod`與`#tag`；當時原Name/Tag/Mod selector改成持久化的Auto/EMI/EMI two-way Search Sync。舊client config當時明確遷移到Auto，payload protocol升至1.2；client-only EMI接線只使用公開API。Automated gate為SelfTest 223325、base 374、addon 17、Mekanism 20、Iron Furnaces 3、Farmer's Delight 7、Python 231、build/EMI minimum+latest compile/runData/JSON/diff全綠；0.1.40 exact-SHA已部署。睡眠下的primary-monitor failures及一次到期Prism background auth refresh都被runner fail closed並清理；current auth-free artifact `20260723-230329-crafting-fuel-page`已到READY，user-owned fullscreen verdict仍是唯一剩餘release gate。
- 2026-07-23 Craftable/Mekanism crash修復：0.1.38的preloaded GUI run雖到達READY，實際切Craftable後仍因完整序列化第三方Item prototype defaults而在`mekanism:basic_bin`崩潰。Item universal-key variant現改用相對prototype的`DataComponentPatch`，不再顯式編碼可能不可序列化的default component；新增遍歷fixture全部Mekanism Items的round-trip regression。Final automated gate為SelfTest 223323、base 374、addon 17、Mekanism 20、Iron Furnaces 3、Farmer's Delight 7、Python 230、build/EMI compile/runData/diff全綠；0.1.39已部署且SHA-256一致。`build/gui-runs/20260723-212132-crafting-fuel-page/`已用唯一0.1.39到達READY；current fullscreen Craftable互動仍由使用者在該handoff確認。
- 2026-07-23 terminal readability + scenario-isolated Prism follow-up：修正machine rate分數/低速率誤導、recipe available/required數字截斷、chemical alias落入隱藏Other、無provider仍顯示Gas/Other，以及Auto Storage越權註冊第三方EMI workstation。Prism schema 5改成scenario-scoped blocks/functions/state；crafting support transaction載入TMRV/Mekanism並拒絕JEI衝突。GUI handoff的重複性setup已再自動化：world直接生成一份matching Core repository/BE reference，預載代表items、所有vanilla/optional stations、Fuel/process/axe reserves與fluid/FE/chemical，玩家inventory為空且只需頁面/recipe目視，不再逐項搬運。原0.1.38 automated gate為SelfTest 223323、base 374、addon 17、Mekanism 19、Iron Furnaces 3、Farmer's Delight 7、Python 230、build/EMI compile/runData/diff；`build/gui-runs/20260723-205432-crafting-fuel-page/`雖以唯一live record到達READY，但後續Craftable互動崩潰，已由上方0.1.39修復項取代。

## Backlog

- Typed-resource Core / GitHub #9 foundation已完成：one-ledger item/non-item domain、raw-preserving persistence、public addon kind/block/container/capability/transaction、typed Terminal與held-container transfer、generic/native active/passive Bus、Creative unlimited、typed exact filter與deterministic N-input/N-output family均已接線。Storage view的Item/Fluid/Energy/Gas/Other selector也已完成；後續產品工作只剩bounded multi-step planner，provider缺席raw payload與no external-machine send-and-wait規則不變。完整契約見`docs/typed-resource-storage.md`。
- 擴充方塊(第二階段,PLAN §四)。
- Tier 3 遠端存取完整體驗(背包開關、遠端 GUI 優化)。
- Recipe scope expansion：exact component-matched/preserving `SmithingTransformRecipe`、exact component-preserving `SmithingTrimRecipe`與deterministic vanilla default-state axe strip/scrape/wax-off已支援；mod block tool hooks、special/dynamic/incomplete或缺少registry context的variants、event/player/world-state-dependent transforms、loom/cartography/grindstone/anvil/enchanting仍需逐類建立可完整simulate/commit的server contract，否則繼續fail closed。
- Brewing production path:`BREW_ENERGY` / `blaze_fuel` 已有資料模型,但尚未接可執行的 brewing recipe consumer；`bottle_fuel` 已退役且現行repository不遷移舊NBT。
- Modded recipe adapters：十個built-in exact families、公開`singleItemToItem`/`deterministicResources`、Iron Furnaces station variants、Farmer's Delight Cooking Pot，以及Mekanism Crushing/Enriching/Smelting/Combining/Pressurized Reaction均已完成。Custom exact family可宣告1–9個consume/catalyst/tool alternatives與multiple primary/remainder item/fluid/power/chemical/addon outputs，並走同一Core transaction；若有item primary則它是selection identity，否則必須恰有一個typed primary。Chemical-only primary保存exact key與long per-craft amount，只能Storage commit；Player/Cursor/EMI inventory與overflow fail closed。`Recipe#getIngredients()`generic fallback、chance/world-state inference與external-machine send-and-wait仍禁止。下一步是其他可證明deterministic families與bounded multi-step server graph。完整契約見`docs/typed-resource-storage.md`、`docs/recipe-family-api.md`與`docs/superpowers/plans/2026-07-14-modded-recipes-multistep-crafting.md`。
- Multi-step magic crafting：最低支援EMI 1.1.24的公開API可查recipe並開tree screen，但建立/讀取`BoM`/`MaterialTree`是internal API，不能作server planner。keyed station descriptors與normalized built-in adapter contracts已完成；下一步才是bounded/cycle-safe、exact-component、energy/remainder/capacity-aware的server graph與整筆atomic net transaction。EMI僅作client tree/selection UI；首版不含queued jobs/crafting CPU/monitor。
- Optional Bus compatibility follow-up：Phase 1–4的versioned config、server menu/security、directional/directionless matrix、generic/native typed transfer、safe one-slot Export view、structured re-entry guard、persistent escrow、Wrench/drop lifecycle與adversarial/addon/Mekanism fixtures均已完成。未來若值得增加present-mod CI，優先加入一個代表性的Sophisticated Storage item-capability job；只作相容性證據，不釘玩家版本、不暴露raw Core handler。契約見`docs/superpowers/specs/2026-07-17-bus-automation-contract.md`。
- Craft planner 完整性 hardening：目前 deterministic allocation/packing 已安全 fail closed，但後續要把 ingredient source 與 destination 聯合規劃，避免「改吃玩家材料即可騰出格子」仍被拒絕；primary output 與 container remainder 也要共同 packing，讓既有 Core stack/new inventory type 的可行交換不被固定順序漏掉。
- NeoForge 一般 deprecation migration(低優先):`ClientSetup` for-removal bus 用法已清除；剩餘 `Level.hasChunkAt`、item registry holder 與 GameTest `RegistryFriendlyByteBuf` 舊 API 要在不改 unloaded-chunk 行為下遷移。
- Build/GUI tooling cleanup(低優先)：提供不 bump 版本的 transactional exact-current Prism deploy CLI，並讓 runner error message 分流。
- 舊 Art metadata portability(低優先)：`20260713-*` 等較早 `art/texture-generation/**/*.json` 仍保存本機 `/Users/swear/...` 絕對路徑；runtime jar 不含這些 metadata。新的 `20260714-terminal-family/selection.json` 與 normalized metadata 已使用 repo-relative provenance，舊批次日後有需要再遷移。
- Modrinth / CurseForge 發布：0.2.0-alpha workflow、metadata、dependency matrix、fail-closed `publishing` environment與三平台upload均完成；平台listing審核後只需以immutable GitHub asset hash核對公開下載。

## In Progress

- macOS F11 visual regression contract：0.1.22 replaces monitor-backed macOS F11 with `minecraft_macos_borderless_fullscreen`; `MacOsWindowMixin` creates a borderless Cocoa window without attaching GLFW to a monitor or selecting a display mode. `prepare_prism_gui_world.py` records the desktop mode, and the runner refuses handoff if its READY-time check differs. The current fullscreen result is user-confirmed; every later visual change still requires its own current-run checklist and F11 → titled windowed window → Command-Q close gate. Historical 0.1.17–0.1.20 fullscreen verdicts remain evidence only, not current acceptance criteria.

## Recently Done

- 2026-07-23 optional recipe/Craftable GUI repair：Fuel machine slot先由自訂tooltip取得single ownership，避免Iron Furnace原版物品tooltip與rate/storage文字疊畫；EMI plugin從Furnace descriptor註冊所有非原版variants為Smelting workstations且不link optional compat class。Typed item recipe現在在啟用Use Player Inventory時，讓Craftable listing、preview、Max、exact max-flow、delivery simulation與commit revalidation共用Core + 可見36格玩家來源；Core-first分配、exact player-slot reservation與full-destination rollback由Farmer's Delight fixture守護，non-item typed resources仍Core-only。Items selector保留Chest，玩家背包來源改用Bundle。Final GREEN為build、EMI minimum/latest compile、SelfTest 224741、base 372、addon 17、Mekanism 19、Iron Furnaces 3、Farmer's Delight 7、Python 225、runData no-drift與diff check；0.1.37 build/deployed SHA-256同為`a28026dc49e1e59e61b4543dc445ab9dfa140d58c91fdcb605af1d3dec455d3c`。current fullscreen verdict仍由上方macOS gate待使用者確認。
- 2026-07-23 GitHub #1/#10 polymorphic stations + real optional recipe integrations：logical descriptors可同步1–64個exact item variants與各自normalized rational rate；Core依descriptor/item/rate持久化fractional carry，preview以actual installed variant為首並每40 client ticks輪播。Iron Furnaces把Copper–Million normal furnaces加入vanilla Furnace slot並從live cook-time config推導速率；Farmer's Delight Cooking Pot與Mekanism Crushing/Enriching/Smelting/Combining/item-output Pressurized Reaction使用bounded typed family、station work、Fuel/FE與atomic rollback。Public API另支援explicit deterministic special-recipe opt-in、overlapping alternatives max-flow與alternative-specific remainders。五組GameTest在CI/release由各Gradle task以root-bounded gate清除`run/world`後串行；final GREEN為build、EMI minimum/latest compile、SelfTest 224741、base 372、addon 17、Mekanism 17、Iron Furnaces 3、Farmer's Delight 5、Python 218、runData written 0/no-drift與diff check。代表性optional-mod artifact不是玩家exact pin；GitHub evidence已更新。依使用者要求本批未啟動Prism，current fullscreen visual approval仍集中到全部issue完成後一次執行。
- 2026-07-23 GitHub #6 Bus Phase 2–4 implementation：schema-2 exact typed filter、whole-snapshot server menu、owner/operator/stale-revision security、mode/side/null invalidation matrix、directionless neutral model、Wrench claim/rotation/dismantle、structured actor guard、generic+native typed front discovery、safe one-slot Export item capability及persistent typed escrow已完成。Terminal另可用public container strategies在cursor上原子存取fluid/FE/chemical/addon resource，並在Core/cursor mutation前拒絕spectator與失效menu。Cached wrappers每次call重驗current BE/escrow/path；active item與typed transfer都用immutable plan，external callback後會拒絕replacement endpoint。Export detached target拒絕反向reclaim時保留exact差額；per-Bus recovery UUID與HIGHEST-priority drop protection守住同位置快速移除、later listener/event cancel與取消Wrench event。完整CI等價GREEN為build、EMI minimum/latest compile、SelfTest 224723、base 371、addon 14、Mekanism 5、Python 217、runData 0 files written/no-drift與diff check；新Bus GameTest跨delay污染全域`doTileDrops`的假紅也已隔離並重跑通過。0.1.35 exact部署已完成（SHA-256 `639160a638a9b614f23abe751b6ffd5f6374b8c0bc45f3c30fd860bfff7b4a67`）；code已commit，`bus-configuration` current fullscreen verdict仍與其他視覺變更一起留在上方macOS gate。
- 2026-07-22 RS2-style terminal preferences：以RS2 client-global grid config為主，sort/order/search synchronization/resource view與Crafting page/use-player-inventory/output/Transform Target改由physical-client全域`auto_storage-client.toml`持久化，所有實體/遠端終端共用；每次開啟仍由server menu驗證並用既有data slots確認。Session acknowledgement避免初始menu defaults回寫覆蓋config；ack前另以持久偏好作純畫面overlay，Storage/Crafting不再於逐格data-slot同步期間閃過預設頁與控制狀態，且不改寫任何menu/Core資料。search text、scroll、selection、visible rows與全部Core資料不進client config。新增純邏輯/wire/static與兩個GameTests，base gate提高為337；偏好加入`TerminalSettingsPacket`後payload protocol當時升至1.1；2026-07-23 search ordinal語意變更後當時升至1.2，2026-07-26 resource-view ordinal使協定先升至1.3；2026-07-27 descriptor logical label wire使其升至1.4；同日All聚合resource-view ordinal使現行值升至1.5，舊client/server組合會明確拒絕而非誤讀封包。First-run空config的NeoForge correction另有SelfTest守住`fuelTarget=auto`，避免immutable候選清單的`contains(null)` NPE。Prism runner仍要求11.0.3+與已完成初始化、保持開啟的normal-root Prism process，再依官方CLI使用既有owning account與dev instance；沒有warm process時會fail而不cold-start auth；fabricated Offline-only `-d` root已因會進Demo而移除。runner不改accounts，並以本次launcher log的實際auth step/endpoint fail-closed。current fullscreen handoff `20260722-124903-terminal-left-rail` 已由使用者確認重開終端與fresh client啟動後不再閃過預設值。
- 2026-07-17 GitHub #6 Phase 2b：`BusFilterPolicy`與automation gate已接到source-slot-order active Import、simulate/execute一致且不修改caller stack的passive Import，以及rule-first active Export。Runtime config load清transfer cooldown使新policy立即生效；Export drop保存owner-stripped item/tag/mod設定。五個新增GameTests經RED後為317/317；configuration packets、ownership、capability invalidation與directionless仍未完成。
- 2026-07-17 GitHub #2 Smithing Trim exact slice：第十個built-in adapter支援exact concrete `SmithingTrimRecipe`。Template/base/addition都綁完整component identity，輸出由vanilla `assemble`保留base components並套用registry解析的trim pattern/material；同recipe ID的exact output variants不合併，canonical source ordering不依Core/hash iteration，65,536組合cap超限即整體fail closed。Lookup/select/current/reload/commit都用current holder + selected exact output重解variant；missing station/context、stale holder、full destination與output rejection均no-op/rollback。Transform也改為source-bound variant但保留generic result lookup。Special/dynamic/incomplete與缺少registry mapping仍unsupported。
- 2026-07-17 built-in recipe-adapter authority：最初九個 exact adapters 完成 ordered inputs/multiplicity、stable station descriptor、process/Fuel/tool cost、checked output/remainders、presentation與simulation/commit validation。Crafting menu只保留通用allocation、delivery、capacity與atomic rollback，所有操作按ID重取目前holder並重新classify；舊station/energy family tables已移除，第三方family與多步驟仍fail closed；上方GitHub #2 slice其後把現行總數擴為十個。
- 2026-07-17 GitHub #6 Phase 2a：`BusFilterPolicy`將ordered raw rules編成immutable slots，保留ACTIVE/DUPLICATE/UNAVAILABLE狀態；四種rule、Allow/Deny empty、unsupported fail-closed與決定性候選順序由六個GameTests守護。Component次序使用registry-aware codec + canonical NBT，而非`DataComponentMap.toString()`或來源collection iteration。後續Phase 2b已接directional transfer；玩家config UI仍未完成。
- 2026-07-17 GitHub #6 Phase 1：Import/Export Bus加入schema 1 `busConfig`與精確legacy defaults，unknown/malformed config原樣保存且disabled；四種rule可round-trip component identity及缺席registry raw資料。新放置Bus記錄owner，Export drop保留config/filter但移除owner/runtime Core hints；`BusActor`完整攜帶owner marker、dimension、position、network、direction與operation ID。八個新增GameTests實際註冊後總數297/297，static guard防止後續test只編譯卻未註冊；此slice不啟用directionless/filter/menu/security behavior。
- 2026-07-17 GitHub #8 Creative Storage Unit：新增創造模式/指令專用、無生存配方的獨立方塊/物品；`StorageTypeCapacity` 以有限槽加 explicit unlimited state 取代 sentinel，Core full/incremental topology、insert、craft output planning、menu sync與雙語容量顯示共用同一語意。有效已載入單 Core 網路的一個或多個 Creative Unit效果相同；移除、斷線、卸載、Wrench或爆炸只回到既有超額狀態，不刪物品。雙語item tooltip明示無限種類且不生成物品，Fuel無限時改畫Creative Unit icon。Replicate login-shell workflow以seed 71425產生候選，再沿既有16×16 family pipeline完成infinity-cell runtime texture、80×16 Fusion `pieced` sheet、12-block connection models、manifest與contact sheets；無placeholder。Prism lab schema 4已把Creative Unit加入active network、isolated/connected galleries與player kit，checklist可驗∞/tooltip/native/Fusion；本次不啟動Prism，current fullscreen manual verdict仍待完成。
- 2026-07-15 macOS borderless F11 implementation：`MacOsWindowMixin` intercepts Minecraft `Window.setMode` on macOS, turns F11 into a Cocoa borderless window, hides Dock/menu bar, and never calls GLFW monitor attach for the fullscreen path. The preparer removes `fullscreenResolution`, preserves 1280×720 only as windowed fallback, and captures desktop point/pixel/refresh/depth mode; the runner compares it at READY and fails closed on mismatch. 0.1.22 was deployed to Prism dev; the live `terminal-left-rail` session kept 1470×956 points / 2940×1912 pixels / 60 Hz and the user confirmed the fullscreen result. The close contract and exact-PID watchdog artifacts are documented in `docs/macos-fullscreen-guide.md`.
- Historical 2026-07-14 macOS F11/shutdown baseline：the old monitor-backed F11 path and direct fullscreen Command-Q could produce a 0×0 Java window/stall. Its exact-PID cleanup and `shutdown.json` watchdog remain, but its “monitor current mode” fullscreen mechanism is superseded by the 0.1.22 borderless implementation.

- 2026-07-14 connected-progression Task 7 automated/deployment/handoff：Fusion mod從錯誤的legacy AdditionalRuntimeClasspath移到只供client/data runs的隔離`fusionRuntime` source set，`runData`真實載入1.2.12而dedicated GameTest不載入；Gradle publishing URL space-assignment warning也以獨立RED修正。Final evidence為Python 146、compileJava/build、SelfTest 129163、GameTest 269、runData no-drift、runClient dry-run、connected/Patchouli/jar inventory與diff check。部署器bump至0.1.18，build/deployed SHA-256均為`d4219e441668b0262cbb6247ec0f796d4cf3c5fa9738b1eb47173783c15764bc`，Fusion SHA-512符合pin；`build/gui-runs/20260714-211556-crafting-fuel-page/`已READY並保留唯一current client，visual verdict仍由使用者在fullscreen完成。
- 2026-07-14 shared-terminal Task 10：live Bottle Energy 已退役，舊 `bottle_fuel` 以 plain Glass Bottle + overflow escrow lossless migration，refill 延後至 outermost mutation batch 結束以保護 crafting transaction；Import Bus 保留 front pull並新增 all-side insert-only passive capability，active/passive missing-Core lookup共用10-tick negative cache。Fuel popup、neutral no-recipe、dim station badge、Player Head/Core destination icons、concise localized tooltip、exact en/zh parity與71422–71424對稱/差異化/directional textures均完成；review另補回Auto tie-break contract並修正繁中Brew Energy語意。Final automated evidence：compileJava/build、SelfTest 222956、GameTest 262、Python 110、runData no-new-drift、runClient dry-run、78 JSON與diff check全綠。0.1.17 已原子部署，build/deployed SHA-256 同為 `3da6203ba16e3ad0a5b090955e5214dbf4fb33415c8b0bb06620fadf26caa93e`；fullscreen verdict仍待後續。此處 Bottle Energy/escrow migration 僅是歷史行為，已由 2026-07-17 no-migration repository revision 取代。
- 2026-07-14 shared-terminal Task 9：active-doc consistency audit 用新 static RED 抓到 manual GUI log 仍鎖死歷史 SelfTest/version，改為 zero-failure 正則並禁止 current 流程釘死特定 patch。0.1.16 的 compileJava/build、SelfTest 222959、GameTest 251、Python 104、runData no-new-drift、runClient dry-run、78 JSON/model/texture/jar 檢查已綠。Deploy script 原子部署唯一 jar，build/deployed SHA-256 同為 `2ca800b1edbf643f278064d1d3392414fc2c48b529a1003b3e2ce7d6fbbd350a`；目視 verdict 尚待 current-run fullscreen handoff。
- 2026-07-14 shared-terminal Task 8：RED抓到2張unreferenced bus PNG、obsolete `grid.png`/`crafting_grid.png`、32×32 orphan與missing reproducibility manifest。GREEN以Replicate `rd-fast` seed 71421、selected chassis 04、0.38/0.68 img2img、14色palette與semantic cleanup建立16個model-referenced native assets；Core/Storage/Crafting、六段tier bars、blue inward Import、orange outward Export、Remote與16px white control atlas現在共用明確family。Predictions/prompts/portable metadata/assembly/contact sheets只在`art/texture-generation/20260714-terminal-family/`；runtime只留實際引用PNG。Focused Python 2/2、full Python 103/103、build與jar inventory已綠；fullscreen外觀仍待Task 9。

- 2026-07-14 shared-terminal Task 7：`TerminalLayout.PopupList` 從 ordered server-approved targets 建立 Auto+targets rows、最多六列 viewport、clamped scroll與 selector/list/popup geometry；shared cycle方向不變。Popup顯示代表物/名稱/selected state，選取、Escape、外部點擊、離頁都關閉並清 focus，開啟時加入 EMI exclusion，且覆蓋區直接攔截底層 slot click/tooltip。Machine/reserve hover由 whole flow cell縮到實際 18px station slot/16px representative icon。RED涵蓋 missing geometry API、cycle-only selector、whole-cell hover、overlay click-through與舊 Prism checklist；GREEN為 compileJava/build、SelfTest 222959/222959、GameTest 251/251、Python 102/102、runData no-new-drift、runClient dry-run。Fullscreen popup/selected-state verdict仍待最終 gate。
- 2026-07-14 shared-terminal Task 6：`MachineEnergyTable` 新增 PROCESS/INSTANT/CONSUMABLE categories；五台 process machine 維持 stack `N` 產生 `N/tick`，Crafting Table/Stonecutter/Smithing Table server-side max one且可取回，axe slot 改成 transient conversion input。`AxeEnergy` 只接受 axe abilities，finite value 精確為 remaining durability × (Unbreaking level + 1) × count；Mending/其他附魔不加值，checked overflow/已 infinite/conflict 都原樣拒絕。Core NBT 持久化 finite long + explicit infinite flag；Unbreakable canonicalize 成 ∞，後續 axe拒絕，recipe不遞減；legacy slot 8 僅成功轉換後清除，失敗時仍由 Axe Energy 格顯示並可取回；legacy instant overstack保留一個並將 extras 回收至 Core。Synthetic strip/scrape/wax-off preview/presentation/Max/commit 全部改用 exact Axe Energy，resource metadata有 explicit infinity bit，blocked output不扣值，per-key long-count mutation一次處理到 `Long.MAX_VALUE`。Crafting parity在該階段為149 slots/100 data slots；現行固定descriptor bank、Creative容量與resource view同步後為396 slots/102 data slots；void GUI lab另提供 plain、damaged Unbreaking II與 Unbreakable axes，checklist不再要求取回/重裝 axe。初始 RED 為 66 個 missing-API compile errors、Fuel UI static failure與兩個 GUI-lab/checklist failures，review RED另涵蓋 migration recovery/infinity/bulk commit；GREEN 為 compileJava/build、SelfTest 222949/222949、GameTest 251/251、Python 99/99、runData no-new-drift、runClient dry-run。Fullscreen finite/∞ visual verdict仍待整批最終 gate；legacy slot/overstack migration 僅是歷史行為，已由 2026-07-17 no-migration repository revision 取代。
- 2026-07-14 shared-terminal Task 5：新增與 EMI `CraftingDestination` 分離的 direct-terminal `TerminalOutputDestination`，預設 Player，可由 shared rail server-synced 切到 Storage。Player 在扣除 player-source ingredients 後先填 36 格再把 overflow 放 Core；Storage 的 primary outputs 與 crafting remainders 全部只進 Core。完整批次先做 long-safe capacity planning，exact `Long.MAX_VALUE` 可接受、少一格/溢位整批 no-op；execute-time output 拒絕會 rollback Core ingredients。`×1/×8/×64/Max` 全部服從 terminal toggle，EMI Cursor/Inventory 仍服從 request destination；ingredient source 與 output destination 互不改動。Crafting parity 為 149 slots/95 data slots。RED 為 34 個預期 missing-API compile errors及 2 個 focused static failures；GREEN 為 compileJava/build、SelfTest 222946/222946、GameTest 238/238、focused Python 3/3、full Python 98/98、runData no-new-drift、runClient dry-run。Shared rail 視覺仍待整批最終 fullscreen gate。
- 2026-07-14 shared-terminal Task 3：新增 immutable `RecipePresentation`/kind；server 以 active exact holder 同步 ID、kind、dimensions/positions、shapeless、exact output components/count、station、aggregated item rows 與 energy/tool rows。Crafting menu 維持 94 data slots、hidden presentation 擴成 22 格/總 slots 149；client 只重建 model，不掃 RecipeManager/Core。Workspace 由固定九格 resource grid 改為 diagram→0..12 variable ledger→footer，item neutral、energy/tool dark-red，output decoration顯示實際每次產量。RED 為 28 + 31 compile errors、4 focused static failures及 3 個額外契約 GameTest failures；GREEN 為 compileJava、SelfTest 222946、GameTest 231、focused Python 43/43，fullscreen gate 留到整批完成。
- 2026-07-15 EMI compatibility policy：release metadata改為client-only required`[1.1.24,2)`，minimum dev baseline仍是`1.1.24+1.21.1`，CI/release編譯minimum+latest compatible，manual client-smoke用isolated Gradle task下載latest compatible；full EMI只在client/data `fusionRuntimeRuntimeOnly`，dedicated server不要求也不載入，compat Java禁止internal packages。
- 2026-07-14 shared-terminal Task 4：dev `runtimeOnly` 載入 full EMI；當時release metadata保持optional，已由2026-07-15 policy取代。Base screen/native path 無 EMI import，`ModList` guard 後才載 compat bootstrap。Exact standard recipes 經 public `EmiRecipe#addWidgets`/bounded holder/widgets渲染，input/tooltip只在 diagram bounds，internal axe及缺少 public representation才 native，unexpected exception不 catch-and-fallback。5 focused RED 後，compileJava/build、SelfTest 222946、GameTest 231、focused Python 48/48、full Python 95/95、runData no-new-drift、runClient dry-run全綠；fullscreen EMI widget gate 尚待整批完成。
- 2026-07-14 shared-terminal Task 2：Storage/Crafting 改用唯一 `TerminalLayout.forProfile(...)` 與 capability-based profiles，base shell 成為 common grid/search/scroll/rail/focus/amount/control owner，Crafting 只加 page/source/recipe/Fuel capabilities；移除 hide-and-replace、Unicode/procedural rail glyph 與 per-value count scale。所有 cycle controls（含 Fuel target）支援 left-next/right-previous/wheel-down-next/wheel-up-previous；18×18 buttons 共用 16×16 item/`terminal_controls.png` atlas。RED 是 41 compile errors + vanilla Fuel CycleButton static failure；GREEN 為 compileJava、SelfTest 40999、GameTest 223、focused Python 37/37、full Python 84/84，fullscreen gate 尚待後續整批功能完成。
- 2026-07-13 true-void GUI lab replacement：`New World` 降為 1.21.1 metadata template，overworld 改 exact flat-air void 且 strip copied chunks/dimensions/player/data + embedded `level.dat/Data/Player`；Y=80 37×25 platform 含 active Core/terminals/T6→T1/buses、isolated texture gallery、Core/station/energy baseline、deterministic hotbar/inventory、3-tick reset latch。Python 76/76、build、SelfTest 40608、GameTest 222、runData/datagen no-drift 全綠；current offline handoff READY，舊 orphan client 已關，只留本次 Minecraft 給使用者。
- 2026-07-13 station/recipe/output/texture implementation：Core equipment 5→9；station-gated crafting/cooking/stonecutting/exact Smithing Transform/internal vanilla axe actions；Smithing component predicates exact-match 並保留 base components；axe 每 craft 1 raw durability；inventory-first/Core-overflow/full rejection atomic output，含跨 `Integer.MAX_VALUE` 與 largest-deliverable Max；Installed Stations two-row flow + lower-right control panel；native server-synced EMI-like recipe workspace；16 個 model-referenced textures 全部 16×16，metadata/preview 移出 runtime tree。0.1.15 final automated evidence：build、SelfTest 40608、GameTest 222、Python 72/72、runData、JSON/datagen no-drift；21:24 已部署 exact jar 並產生 current-run handoff artifact，fullscreen 目視仍待使用者判定。
- 2026-07-13 Task 9 Fuel selector/layout implementation：0.1.12 實機失敗截圖驅動 RED；SelfTest 鎖 selector/rail geometry、稀疏與 partial final pages，GameTest 鎖 retired 17/18 no-op，Python 鎖 vanilla CycleButton、無動態 next-item action icon與新 checklist。Production 只保留 server Auto/exact IDs，Fuel rail 只畫三頁籤，selector wheel 優先於 panel paging，每頁可見 descriptors 用比例邊界鋪滿 bounds。SelfTest 40406、GameTest 210、Python 70/70、build/runData/77 JSON/datagen no-new-drift 全綠，exact final 0.1.14 jar SHA-256 `97069ca985797ce237b421df2fc5c08c44cc2ca4dedf1957d0f756c2c92dae75` 已部署，待 fullscreen gate。
- 2026-07-13 Task 8 fullscreen verdict：0.1.12 automated gates/deploy 成功，但 Fuel 排版/target navigation 仍失敗，由 Task 9 replacement 取代 visual contract；證據在 `build/gui-runs/20260713-001205-crafting-fuel-page/user-feedback-fuel-layout.png`。
- 2026-07-13 Task 8 implementation：依 RS2 `ItemGridResource` + `ResourceSlotRendering` 模式自行改寫 compact K/M/G/T/P/E formatter、16px normal/0.5-scale cell-local right alignment；全部八種 energy 改用對應 machine/fuel item，Fuel 顯示 server-synced stored/max types。新增 formatter boundary、完整 representative metadata、render static、checklist、94-slot wire sync，以及 open Fuel 頁 type count/capacity live refresh GameTest；該測試先以 `menu=10/core=35` RED 抓到 topology stale metadata。SelfTest 40340、GameTest 209、Python 70/70、build/runData/77 JSON/datagen no-new-drift 全綠，exact 0.1.12 jar SHA-256 `f7b0e78e630c03f77331fa9aad4c4d3f04512d069b327c9bad6942a0c8844309` 已部署，待 fullscreen gate。
- 2026-07-12 Task 7 three-page/scalable Fuel implementation：Craftable toggle/state/data slot 完整移除，menu extra 83/total 94 data slots 與 133 slots 真實 signed-short parity；Storage 可抽取，Craftable synthetic 可選不可抽且不供 EMI 當 client inputs，Fuel fail closed。Max 每次 server fresh preview，固定量不足整次 no-op，且可耗盡超過 9,999 次的合法材料。Fuel panel 使用完整 frame inner width，machine/reserve descriptors 產生 cells/capacity/pages，off-page slots 重建至畫面外；target rail 固定六個 buttons，不隨 descriptor 數量出界；rail groups、shadowless values、semantic search icons 與 focus cleanup 有回歸。SelfTest 40321、GameTest 208、Python 69/69、build/runData/77 JSON/datagen drift 全綠，exact 0.1.11 jar SHA-256 `09fd5ca4e2b2d41c1163daa255da58232be021f1a131f30e53d80748f37b6d1d` 已部署，待 fullscreen gate。
- 2026-07-12 Task 6 fullscreen correction implementation：Crafting recipe pane 顯示最多九個 item predicates 及 process/Fuel 的 current/required-for-one；95 data-slot 真實 signed-short parity 保留 133 menu slots；component variants 永遠使用完整 `ItemKey`；layout 在 415/416 scaled-width boundary 自動切 narrow/side-by-side、423×291 代表尺寸保留 16px 水平 margin，rail+frame 整組置中；Crafting rail 改用相同 18×18 buttons/12×12 icons。SelfTest 39823、GameTest 205、Python 68/68 與 build/datagen/JSON/diff/drift checks 已綠，exact 0.1.10 jar 已部署，待 fullscreen gate。
- 2026-07-12 post-0.1.8 Fuel/Craftable/EMI implementation：Fuel 玩家名稱統一為 Fuel，runtime `getBurnTime(null)` 接受 logs/modded fuels；爐系 preview/execute 共用 concrete `cookingtime`；server recipe catalog 產生 zero-storage Craftable outputs；EMI exact `{recipeId,amount,destination}` 支援 NONE/CURSOR/INVENTORY 單層原子合成。0.1.9 fullscreen review 證實 craft 成功，但否決 320×240/≥480 raw-breakpoint 版面、空洞 recipe workspace、Compact Grid 與不等大 Unicode icons；由 Task 6 replacement 取代該 GUI 契約。
- 2026-07-12 Fuel/machine + GUI/deep correctness baseline（0.1.8 已部署）：移除免費 process-energy auto-fill；Core 持久化五種 machine stacks，`N` 台精確 `+N/tick`，conflict 暫停但可取回；menu 133 slots/52 data slots；所有 view/target controls 移到左 rail，recipe panel 42→70px，post-vanilla-click 清 focus。Automated gates 全綠，但後續 fullscreen review 否決其 Fuel naming/value、Craftable candidate source 與視覺/自適應品質；只可視為 baseline，不是完成的 GUI release gate。
- 2026-07-12 dedicated Fuel page baseline（0.1.7）：完成 server-owned page/target、專用 input、八個 long wire、Auto priority、remainder 與 EMI rejection；本次 revision 已取代其舊的免費 Machine Energy 與 Furnace/Blaze 顯示方式。
- 2026-07-11 final audit follow-up:crafting 只接受 exact static vanilla recipe classes，component predicates 保留各自身分，玩家材料只掃 36 格主背包/快捷列；preview/execute 都從目前 `RecipeManager` 解析 recipe id，datapack reload 移除/替換的 cached holder 不可顯示或執行。stacked container fuel 逐件返還 remainder，energy event 即時更新 open preview，Remote rebind 保留 unrelated `CUSTOM_DATA`。Prism runner 清 wrapper 後 native offline launch 並 handoff 給使用者，log rotation 不漏錯；GUI world copy 與 Prism jar deploy 改 staging/rollback transaction。SelfTest 104 + GameTest 148 + Python 38。
- 2026-07-11 second correctness pass:terminal fuel conversion 會歸還 lava bucket 的空桶；fuzzy variant、重複 NBT entries 與 Core+player preview 加總皆飽和 `Long.MAX_VALUE`；EMI handler 只接受支援的 backing recipe contract，送 exact recipe id packet 並由 server 同時驗 container/display output/recipe；Smithing 明確 fail closed。Network block 的 `onPlace` 合併到 next-tick topology pass，`moved=true` removal 也會清舊 cache，並守住 incremental/full-rebuild depth boundary。Import/Export bus 在 reentrant capacity/conflict 造成 partial commit 時會精確 rollback，拒收 remainder 則顯式掉出而不吞物。SelfTest 104 + GameTest 141。
- 2026-07-11 correctness + security audit:terminal grid scroll metadata/live listener refresh/local disconnect validation;search packet length + malformed tag fail-closed;item/energy long overflow saturation;all network blocks use state-aware `onRemove` rebuild;shift extraction preserves legal stacks;crafting rejects conflicted/incomplete recipes, jointly reserves overlapping predicates, rolls back exact sources, and returns item-level container remainders;terminal fuel conversion now reaches `addFuel` through server-validated container buttons and refreshes preview;Core persistent UUID pins remote bindings/menus to the exact same-dimension loaded Core. SelfTest 104 + GameTest 131。
- Historical Core drop foundation + bounded full rebuild：該階段先以vanilla `BLOCK_ENTITY_DATA`取代static same-position fallback，並修正blank Core clone，之後又短暫改成`CoreRecoverySavedData`完整snapshot。兩種payload ownership均已由2026-07-17 world-repository設計取代；Full `rebuildNetwork`仍與core-search BFS共用depth/block guard。當時SelfTest 104 + GameTest 131全綠。
- Prism GUI session runner + optional client smoke:`scripts/setup_prism_computer_use_wrapper.py` 仍可重建 `/tmp` wrapper/app 供診斷；正常 `scripts/run_prism_gui_session.py` 會清空 wrapper、停用 instance error-console pop-up、重建 world，再要求normal Prism已完成account initialization且保持開啟，才用官方CLI `prismlauncher -l dev -w AutoStorageGuiTest -o AutoStorageBot`透過該warm process啟動既有dev instance並自動進入 Minecraft F11 fullscreen；沒有warm process時會在改world/啟client前fail，避免cold-start ownership refresh。runner不建立`-d` root、不改`accounts.json`；READY後只掃本次launcher片段，拒絕Microsoft/Xbox/XSTS/Minecraft-services真實auth step/endpoint，並保存已移除environment dump的artifact，再讀本次Minecraft log/checklist。visual scenario到`AS_GUI_TEST_READY`後停止自動操作並交給使用者完成fullscreen目視，不可再切macOS native fullscreen。`.github/workflows/client-smoke.yml` 手動放入Patchouli、Fusion與exact EMI後啟NeoForge client smoke，HeadlessMC step硬限10分鐘；僅抓client boot/resource崩潰，不取代Prism fullscreen目視。
- Prism GUI true-void lab/CLI flow:`scripts/prepare_prism_gui_world.py` 只取 `New World` 的 1.21.1 metadata，strip 所有 copied chunk/dimension/player/datapack runtime state，將 overworld fail-closed 重寫成無 features/structures 的 flat air void；datapack 在 Y=80 建 37×25 lab、預載 Core/stations/測試 kit，hotbar `1`–`8` 導航、`9` 共用 setup atomic reset，3 game ticks readiness 取代 fixed sleep，且全程無 command block。runner 寫入 `fullscreen:true` 自動套用 Minecraft F11 mode；使用者仍須先通過 mandatory fullscreen gate 才能操作。
- GitHub public repo / CI/CD:新增 public repo https://github.com/swear01/Auto_Storage；CI 在 push/PR/manual 跑 JDK 21 build、GameTest、Python unittest、datagen drift check,並上傳 jar/log/report artifacts；CD 在 tag `v<mod_version>` 時驗版本、重跑 gates、產生 release notes 並建立 GitHub Release。
- 健壯化 pass(地毯式掃描修正):持久化(setChanged、NBT components 存讀、core 掉落物攜帶 BE data)、關閉複製、import bus 吃物、合成回滾、能量溢位、tooltip/焦點初步處理、enqueueWork、BFS/full rebuild 上限等；sticky white focus 的最終修正在本次 post-`super.mouseClicked` revision。
- RS2 設計慣例採用(見 `docs/rs2-design-gap.md` + `docs/superpowers/plans/2026-06-21-...`):fuzzy + joint-reservation 合成配對(A5b)、view 設定 server 同步(A5a/#7)、選取物品身分化(A4/#5)、合成 preview(A7)、Action/Actor storage contract(A2)、StorageListener live refresh foundation(A3)、捲軸/版面修正(#8)。P4 增量網路成長(A1:放置增量 + 破壞 bounded full rebuild 後盾)。剩 P3 增量 grid(低價值,未來若改整列表 client grid 再做)。SelfTest 104 + GameTest 131 全綠。
- Terminal UI 重設計:動態列數(依視窗高度,3–9 列)、排序(名稱/數量/ID × 升降序)、搜尋欄直接解析`#tag` / `@mod`前綴，另有獨立Auto Focus與Off/EMI/EMI two-way Search Sync、程式繪製背景(取代固定 6 列材質,解除 256px blit 限制)、RS2-like 左側 view buttons,以及超過 64 的網路實際數量顯示(含 menu display slot 不再被 `SimpleContainer` 夾回 64)。2026-07-08 曾用 Prism dev 0.1.3 + Computer Use wrapper + macOS native fullscreen 驗證當時版本；這只是歷史視覺證據，現行流程已禁止 native fullscreen 並由 automatic Minecraft F11 replacement gate 取代。
- Patchouli 指南書內容載入修正:localized categories/entries 移到 `assets/.../patchouli_books/guide/en_us`;`book.json` 留在 `data/.../guide/book.json`。2026-07-08 Prism dev `latest.log` 顯示 `BookContentResourceListenerLoader preloaded 17 jsons`,不再是 0;實機開書可看到分類與說明內容。
- RS2 parity P0/P1/P2 pass + regression expansion:Core storage ops 新增 `Action` + `Actor` contract 與 `StorageListener` delta events;bus cached-core 會在斷網後失效;conflicted multi-core 網路不接受也不抽出;增量放置遇 bridge detached segment 或多 core 時 full rebuild/衝突偵測;local terminal `stillValid` 驗證 access block/連通,remote menu pin exact Core UUID + dimension;crafting recipe metadata/preview 由 server sync;craftable-only server filter;EMI 排除 hidden metadata slots;duplicate/overlapping ingredients 共用 joint reservation;real crafting/smelting/player-inventory craft paths有 GameTest。SelfTest 104 + GameTest 131 全綠。
- 方塊 & BlockEntity 註冊、模型、材質(M1)。
- 網路 BFS 掃描、Core ↔ Unit 連通(M2)。
- Storage Unit 六 Tier、`storage_terminal`、能量池系統、`crafting_terminal`、即時/熔爐類合成(M3–M8 大致到位,以程式碼為準)。

## Completed 2026-07-21

- Typed-resource completion：item runtime map已移除；item/fluid/FE/optional chemical/addon kinds共用同一server-owned long ledger、type capacity、persistence與atomic transaction domain。公開`auto_storage:resource_kind` registry、canonical built-in key helpers、generic handler/capability、typed Terminal representatives、Creative全kind無限容量、Import insert-only與Export extract-only generic/fluid/FE/chemical capability均已接線。Mekanism compat只在ModList確認存在後載入；CI只跑10.7.19代表fixture，不把版本釘成玩家依賴，其他版本相容問題由使用者回報。
- Recipe Family A+B completion：supported exact vanilla class/type保持跨namespace零設定發現；公開`auto_storage:recipe_family` registry同時提供bounded `singleItemToItem`與`deterministicResources`。Custom exact family可宣告1–9個consume/catalyst/tool item/fluid/power/chemical/addon inputs、多個primary/remainder outputs，並與built-in recipes一樣走server current-holder重解、checked amount、destination planning及單一Core transaction。Different-package compile fixture與5個獨立addon GameTests驗discovery、public addon kind terminal/capability、mixed commit、retained roles與capacity rollback；這是2026-07-21當時基線，現已由上方Iron Furnaces/Farmer's Delight/Mekanism real integrations與17個addon tests擴充。下一步是其他明確deterministic families與bounded multi-step planner；generic inference與external-machine send-and-wait不做。

## Completed 2026-07-17

- Core out-of-chunk storage repository：`StorageCoreBlockEntity` chunk NBT固定只含`storageId`/`storageSchema`；overworld `auto_storage_core_storages.dat`唯一持有inventory、energy、machines、descriptor與raw unresolved NBT。庫存以每段最多63種的任意多段格式保存，runtime attachment拒絕duplicate owner，missing/corrupt/packed/duplicate狀態全部fail closed。含資料Core的掉落token只指向同一record，claim不複製payload，空Core移除才刪record；舊inline與full-snapshot格式刻意不遷移。
- Prism true-void lab不再注入舊Core block-entity payload；reset放置fresh Core，由server建立repository record，station與axe案例改由player kit實際安裝/轉換。

## Completed 2026-07-15

- 第三方 machine descriptor API：公開 NeoForge custom registry、stable ID驗證、cached server opening snapshot、clientbound consumable state、固定256 menu/Core slots與raw unavailable entry保留已完成；原先parseable stack migration已由2026-07-17 no-migration repository格式取代，built-in recipe-family adapter authority已完成。Recipe Family A+B 的初始完成狀態見2026-07-21項目。
- Superseded Core oversized-drop/recovery中繼版：當時survival、creative、explosion與Wrench會建立完整SavedData snapshot再掉UUID token；現行2026-07-17實作改為永久repository record加無payload recovery indirection。
- 2026-07-28 Ultimate Singularity extreme fixture：真實component-bearing Ultimate Singularity已納入Extended Crafting family，typed exact-input boundary為81；Prism Core預載19種預設Singularity而不再生成假Diamond recipe，recipe ledger以可讀viewport滾動。自動gate全綠，0.1.58 handoff已到`AS_GUI_TEST_READY`；使用者完成F11目視前仍不宣稱GUI verified。
- 2026-07-28 Ultimate Singularity server/client parity correction：0.1.59目視證明server只有9個vanilla inputs而EMI有19個。server現在於catalog prewarm前以Extended Crafting公開registry重載Singularity runtime data；isolated/GUI test datapack補齊10個test-only common-ingot tags，並以component-exact public-recipe matcher守住19個同item不同component inputs。自動gate為Extended 4/4、matrix 3/3、base 398/398、SelfTest 204851與Python 273。0.1.61 exact-SHA已部署，`build/gui-runs/20260728-104109-crafting-fuel-page/`在client/server/sync均載入19種並到READY，current fullscreen capture已顯示完整19-input widget；只剩使用者F11目視判定。
- 2026-07-28 Ultimate Singularity visual correction（由上項取代）：TMRV synthetic-ID/no-backing public recipe改以exact output與完整item-input multiset唯一匹配；162×195 EMI widget在terminal內完整等比縮放，recipe body頂端對齊，ledger短值保持`1/1`單行。0.1.59曾到`AS_GUI_TEST_READY`，但後續使用者截圖已否決該版。
- 2026-07-29 Auto Storage public listing/licensing：Modrinth `auto-storage`與CurseForge project `1630575`已建立；公開名稱及模組display name改為Auto Storage，授權改為MIT並允許第三方distribution。當時repository與技術identity仍是0.2.x狀態，現已由上方0.3.0 breaking rename全部取代且不提供相容遷移。兩平台token已安全保存於GitHub `publishing` environment，`v0.2.0` tag-driven三平台same-jar upload已於2026-07-30完成。
- **0.3.x Compat Kit v2（GitHub #39，complete / merged）**：scanner format 16、runtime probe、migration、typed generation、conformance、custom-resource scaffold、worker package與exact target/ancestry verification皆完成；最後explicit static-import holder、punctuation-only resource命名與empty conformance三個review缺口亦已TDD修正。最終head `a560d256aad624473af23c25dc55b382b0325393`取得Codex clean comment `5154697215`，CI `30728855381`全綠，PR #52 squash merge為`fb63ba4ffdb55b1dc9e339296c3a89e9345e2747`並關閉#39。Developer Wiki同步`93c6edb3b5517f4fab44fa710e08cd692cbc51b3`且未加入Home/sidebar。
