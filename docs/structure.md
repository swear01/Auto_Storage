# Structure

| Path | Purpose |
|------|---------|
| `.cursor/skills/full-repo-audit/` | Cursor Cloud 手動全 repo、唯讀 audit Skill；查證官方與相似專案後，把去重結果發布為單一 GitHub issue；not a PR diff review，也不取代 GitHub-triggered Codex/Bugbot gate。 |
| `.github/workflows/` | GitHub Actions CI/CD:push/PR build + Modrinth EMI minimum/latest-compatible release compile + base/recipe-addon/AE2/Mekanism/Botania/Iron Furnaces/Farmer's Delight/Modern Industrialization/Ars Nouveau/EvilCraft/Powah/Industrial Foregoing/Create/PneumaticCraft/Extended Crafting十五個依序隔離GameTest runs + 一個全選配模組共存GameTest run + Python unittest + datagen drift check + runtime/API/source/Javadoc/Compat Kit與log/report artifacts；full EMI client/data runtime由exact Modrinth version ID供應，optional-mod artifacts各只代表一個CI版本且不成為玩家端exact pin；Botania mutable snapshot另以expected SHA-256守住；`v<mod_version>` tag release重跑同一組 gates，並以`publishing` environment設定將player alpha jar發佈到GitHub/Modrinth/CurseForge，再把API/sources/Javadocs/Compat Kit附加到GitHub Release；手動`client-smoke.yml`放入required Patchouli、釘選Fusion與最新相容exact EMI，再以10分鐘上限跑NeoForge client boot/resource smoke。 |
| `LICENSE` | Auto Storage 原始碼與資產的 MIT License；`jar` task會把它封裝進發布jar。0.3.0 canonical mod ID為`auto_storage`、Java package為`com.swear.autostorage`、repository為`Auto_Storage`，且不遷移0.2.x技術identity。 |
| `art/release/` | Modrinth、CurseForge與GitHub release listing共用的512×512專案icon；不打進mod jar。 |
| `art/texture-generation/` | 貼圖生成 metadata、predictions 與 preview 原稿；`20260714-terminal-family/` 另含 fixed-seed/shared-reference selection manifest、palette/chassis cleanup script與候選/選定 contact sheets。全部刻意放在 runtime resource tree 外，不打進 mod jar。 |
| `README.md` | 公開 GitHub repo 首頁:專案簡介、build/test、CI/CD、GUI 驗證入口、授權狀態。 |
| `docs/releasing.md` | Modrinth/CurseForge listing metadata、`publishing` environment、tag發佈、跨平台SHA驗證、retry/rollback與token rotation runbook。 |
| `src/api/java/com/swear/autostorage/api/` | 不依賴core implementation的公開addon façade/compat-module contract source set。`main`依賴此output；正式player jar合併它，classified API jar再加入受控root contracts。 |
| `src/compat/<mod-id>/` | 十二個bundled compatibility modules；每個`java/`只對classified API jar、對應target與必要companion APIs編譯，不可看完整main output，`compat-module.json`單獨擁有entrypoint、required mods、source set、fixture、預期GameTest數與代表性compile/runtime dependencies。Gradle驗證後生成唯一runtime `META-INF/auto_storage/compat-modules.json`，最終仍合併成單一player jar並在classload前檢查全部required mod IDs。 |
| `src/main/java/com/swear/autostorage/` | Core/runtime Java原始碼；不得直接import任何optional target package。`StorageTerminalMenu`/`Screen`提供共用server-owned grid與layout shell；`CraftingTerminalMenu`/`Screen`只增加Storage、Craftable、Transform、Stations四頁、recipe presentation與output controls。`TerminalLayout`是唯一geometry來源：寬版四頁content bottom一致對齊player inventory上緣，四頁搜尋共用top header位置、focused input router與query parser，裸operator等同空查詢、欄外點擊解除focus，scope只由各頁候選entry決定；Transform左側保留persistent target list並讓source-aware cards填滿其餘高度；Stations依高度計算rows，Processing取得較大且固定左上欄位的三欄flow，Instant使用20px compact icon grid。`TerminalScrollbar`使用14px recessed track及enabled/pressed/disabled vanilla scroller；thumb drag保留grab offset，track按下翻頁並重複，wheel直接跳一個server-aligned row，不保留client動畫狀態。Storage/Craftable使用18列固定menu slot bank，screen依可用高度啟用全部完整列。Stations的第三個utility control切換All/Installed，篩選共用descriptor清單與搜尋結果。client-global偏好由`TerminalClientPreferences`保存，server仍透過103個menu data slots驗證。`CraftableRecipeCatalog`提供server-start/datapack-sync prewarm、`RecipeManager` identity reload invalidation與shared weak candidate index；`CraftingTerminalMenu`每Core只保留一份revision/threshold guarded shared Craftable result，Use Player Inventory繞過cache。`StorageCoreBlockEntity`在attach/insert時保留display-name/component keys並增量維護預設Name index，真正cold open不再重建30k名稱；search與其餘sort仍按需建立，GUI fixture才額外顯式預熱。正向Processing work只在跨越Craftable threshold時使shared result重建。`GuiRuntimeFixture`以marker-gated server command枚舉runtime item registry、安裝全部station descriptors並在handoff前準備Craftable，不建立client storage state。`StorageResourceLedger`/`CoreStorageRecord`統一item/fluid/FE/chemical/Mana/addon資源與WORK transaction，`StorageCoreBlockEntity`每tick合併全部station work後只提交一次。`MachineDescriptor`保存每個installable descriptor的localized logical station-family label；`MachineEnergyTable`把label與variant snapshot一起同步，payload protocol為1.5。UI資源選擇器顯示Processing與All aggregate（tooltip `Show: All`），Mekanism chemical由registry-backed client renderer顯示exact glyph/amount，`station_work`只留作server技術識別。Transform providers與machine variants都由ordered custom registry擁有；bundled optional bridges只存在於各自`src/compat`與fixture。 |
| `src/apiTest/` | 從不同 Java package、只看classified API jar編譯完整公開contract，包含one-call facade、`MachineCategory`、bounded Core/Bus capability bridge與不把client class連進API bytecode的generic resource renderer，防止addon誤依賴implementation output或package-private internals。 |
| `src/recipeAddonFixture/` | 獨立載入的 NeoForge test mod：註冊兩個custom `RecipeType`、legacy `singleItemToItem`與`deterministicResources` families，並以17個GameTests驗station/discovery、polymorphic fractional work與installed-first preview cycle、exact output、overlapping alternatives/remainders、addon kind capability/terminal/held container、mixed item+mana+fluid commit、generic+native Bus discovery、typed rollback escrow、Creative removal、retained catalyst/tool與capacity rollback。 |
| `tools/compat-kit/` | Development-only Compat Kit CLI、strict JSON schemas、Gradle wrapper模板與addon CI範例；`scan/decide/diff/scaffold/verify/publish`把代表artifact/source證據轉成明確contract、故意RED的API-only scaffold、逐task GameTest驗證報告與reproducible release archive。 |
| `compat/audits/` / `compat/contracts/` | 可重現的target surface audit與人工審核後的deterministic recipe contract；下載jar、source checkout、cache與暫存report只放ignored `build/compat-kit/`。 |
| `docs/compat-kit.md` | Compat Kit authoritative maintainer/addon workflow、安全邊界、命令、輸出、TDD與發佈流程；GitHub Wiki鏡像是developer-only，不進玩家手冊導覽。 |
| `docs/ae2-compatibility.md` | 首個Compat Kit dogfood：AE2 19.2.17 Inscriber exact ingredient/press/cost/rate contract、source/SHA evidence、明確排除與7個isolated GameTests。 |
| `src/ae2Fixture/` | 隔離載入代表性AE2 19.2.17與GuideME；7個GameTests驗Inscriber註冊、INSCRIBE/PRESS catalyst semantics、runtime AE→FE cost、work/energy/capacity atomic rollback與Charger fail-closed。 |
| `src/mekanismFixture/` | 隔離載入代表性Mekanism CI artifact的NeoForge test mod：以47個GameTests驗Core/Terminal/Bus chemical capability、全部fixture Item預設components的universal-key round-trip、九個Factory-backed descriptors的basic + 3/5/7/9-process variants與exact throughput、Pressurized Reaction、兩種安全單向Rotary shape、十一種額外deterministic single-block fluid/chemical families，以及chemical-only Storage destination、multi-output、long overflow、atomic rollback與bidirectional Rotary/Nutritional明確fail-closed；版本只作CI證據。 |
| `src/botaniaFixture/` | 隔離載入官方Botania 1.21.1 snapshot、Curios與既有Patchouli；排除JEI並驗expected SHA-256，14個GameTests驗Mana ledger/held Tablet、五個deterministic station families、Elven Trade exact Mana消耗與fail-closed邊界。 |
| `src/ironFurnacesFixture/` | 隔離載入代表性Iron Furnaces artifact：3個GameTests驗supported variants、live cook-time config-derived rates與fractional Core generation。 |
| `src/farmersDelightFixture/` | 隔離載入代表性Farmer's Delight artifact：7個GameTests驗Cooking Pot ingredients、serving container、alternative remainder、cook-time station work、Fuel、player-inventory typed inputs與destination rollback。 |
| `src/main/java/com/swear/autostorage/mixin/MacOsWindowMixin.java` | macOS-only Minecraft F11 interception：以 borderless Cocoa window 取代 GLFW monitor attach，隱藏 Dock/menu bar、退出時還原視窗屬性與 presentation options；非 macOS 保留原版 GLFW 路徑。 |
| `src/main/resources/assets/auto_storage/` | 預設客戶端資源（vanilla模型、lang、blockstates、完整 en_us/zh_tw Patchouli localized content、native 16×16 semantic texture family與16px control atlas）。 |
| `src/main/resources/resourcepacks/fusion_connected_casing/` | 僅在client偵測到可選 Fusion 1.2.12後自動啟用的內建overlay：Fusion connecting models、80×16 five-tile sheets與`.mcmeta`；item models不引用connected sheets。 |
| `src/main/resources/data/auto_storage/` | 資料包(配方、loot、tags,以及 Patchouli `guide/book.json`)。 |
| `src/main/resources/data/minecraft/` | 對 vanilla 的資料覆寫/擴充。 |
| `src/main/templates/META-INF/` | `neoforge.mods.toml` 模板(Gradle 變數注入)。 |
| `scripts/` | Build/CI解析、transactional Prism部署、GUI session與Python regression tools。`prepare_prism_gui_world.py`建立scenario-scoped true-void world與production-format Core record；on-demand `terminal-scale`以`--scale-types 10000|30000`產生exact component variants，再由runtime marker command依實際registry補入每種item各`--items-per-type`個、全部Processing/Instant descriptors與work reserves；玩家背包為空，hotbar 1/2只導向Storage/Crafting Terminal。`crafting-fuel-page`預載optional stations（含Powah Furnator、Ultimate Crafting Table與`Integer.MAX_VALUE` Crusher aggregate）、Processing work、typed resources，以及fluid/gas/energy/mana resources及19種Ultimate Singularity component材料，玩家只保留hotbar 1/2導航及hotbar 3的一顆Coal作Transform Fuel/FE多用途視覺輸入。runner要求warm Prism、official `-l dev -w AutoStorageGuiTest -o AutoStorageBot`、auth/log/display-mode fail-closed、`AS_GUI_RUNTIME_FIXTURE_READY`與F11手動handoff；不建立假offline root或修改accounts。Active world/profile/checklist契約見`docs/gui-test-world.md`。 |
| `docs/gui-test-world.md` | Prism true-void scenario matrix、預載Core/player kit、manifest/NBT assertions、visual assertions與更新流程的active executable contract。 |
| `docs/recipe-family-api.md` | 第三方 exact recipe family registry、`singleItemToItem`/`deterministicResources` factories、typed roles、fail-closed lifecycle與addon fixture。 |
| `docs/addon-development.md` | 公開SDK的no-copy GitHub Release Gradle dependency、one-call façade、全部extension points、bounded lifecycle/capability bridge、失敗語意與alpha versioning政策；這是authoritative source，GitHub Wiki copy每次SDK release必須同步。 |
| `docs/typed-resource-storage.md` | GitHub #9 的通用資源架構：stable kind/resource identity、variant payload、atomic mixed-kind ledger、public handler/block/container strategy contract、Terminal held-container transfer、Bus generic/native transfer與persistent escrow。 |
| `docs/machine-descriptor-api.md` | 第三方 NeoForge descriptor registry、PROCESS/INSTANT/Transform邊界、localized logical family label、256-slot parity、cached server snapshot/state sync、stable-ID persistence，以及descriptor/一般Core庫存raw item NBT原樣保留契約。 |
| `docs/mekanism-compatibility.md` | 代表性Mekanism 10.7.19.85的26個registered recipe types完整盤點：22個exact deterministic支持、四個明確排除、九種Factory variants/rates、其他single-block/world/multiblock邊界與47個fixture gates。 |
| `docs/botania-compatibility.md` | Botania optional linkage、official snapshot/SHA政策、Mana ledger/held Tablet、五個deterministic station families、Elven Trade Mana消耗、排除語意與14個fixture gates。 |
| `docs/modern-industrialization-compatibility.md` | Modern Industrialization optional linkage、14個direct machine families、provider tier/slot/probability邊界、代表性2.5.4 fixture政策與7個atomic typed-resource gates。 |
| `src/modernIndustrializationFixture/` | 代表性Modern Industrialization 2.5.4 + GuideME隔離GameTest fixture；驗證item/fluid/EU/catalyst/multi-output/persistence/rollback與fail-closed邊界。 |
| `docs/ars-nouveau-compatibility.md` | Ars Nouveau optional Source capability、Imbuement/Apparatus exact recipe contract、代表性5.12.1 fixture政策與fail-closed邊界。 |
| `src/arsNouveauFixture/` | 代表性Ars Nouveau 5.12.1隔離GameTest fixture；驗證Source、items、catalysts、remainders、components、persistence與atomic rollback。 |
| `docs/evilcraft-compatibility.md` | EvilCraft Blood流體、Blood Infuser exact recipe contract、Promise tier、XP/Purifier邊界與代表性fixture政策。 |
| `src/evilCraftFixture/` | 代表性EvilCraft 1.2.91 + Cyclops Core 1.29.1隔離GameTest fixture；驗證Blood、tiered recipes、persistence與atomic rollback。 |
| `docs/powah-compatibility.md` | Powah Energizing exact recipe/FE/live rod-rate contract、warm-up/world邊界與代表性fixture政策。 |
| `src/powahFixture/` | 代表性Powah 6.2.10 + Cloth Config + GuideME隔離GameTest fixture；驗證NeoForge Energy、Energizing batching、live tier rates、persistence與atomic rollback。 |
| `docs/industrial-foregoing-compatibility.md` | Industrial Foregoing optional Dissolution/Stonework/Crusher exact contract、live config、unsafe world-state邊界與代表性fixture政策。 |
| `src/industrialForegoingFixture/` | 代表性Industrial Foregoing 1.21-3.6.39 + Titanium 4.0.45隔離GameTest fixture；驗證item/fluid/FE/work、persistence、rollback與fail-closed。 |
| `docs/create-compatibility.md` | Create optional Milling/Crushing/Cutting/Filling/Emptying exact contract、normalized station work與RPM/chance/multi-station邊界。 |
| `src/createFixture/` | 代表性Create 6.0.10隔離GameTest fixture；13個tests驗五個deterministic families、item/fluid/remainder/work/reload與atomic rollback。 |
| `docs/pneumaticcraft-compatibility.md` | PneumaticCraft Air/pressure/heat/multiblock API審計與零production-family fail-closed判定。 |
| `src/pneumaticCraftFixture/` | 代表性PneumaticCraft 8.2.22 present-mod隔離fixture；9個tests鎖住Air與七類machine recipes不被不安全接受。 |
| `src/extendedCraftingFixture/` | 代表性Extended Crafting 7.0.8 + Cucumber 8.0.16隔離GameTest fixture；4個tests驗Ultimate Table gating、9×9/81-position exact commit、one-short no-op、full-destination rollback與真實Ultimate Singularity component identity。 |
| `docs/extended-crafting-compatibility.md` | Extended Crafting shaped-table/Ultimate Singularity支持、81-input boundary、representative fixture政策與fail-closed transformer邊界。 |
| `src/compatibilityMatrixFixture/` | 同時載入十三個代表性optional-mod artifacts的NeoForge fixture；3個GameTests驗registry/resource-kind/accepted-family/Furnace/AE2 Inscriber共存、PneumaticCraft零不安全註冊，以及固定11,657-recipe/10k或30k exact stored-type Terminal gate。真正cold default-Name Storage open、首次Craftable construction與shared Craftable switches都必須<50 ms；一次性alternate sort/search p95必須<250 ms。每menu必須<128 KiB，shared index必須<8 MiB。Fixture另驗單一production record、每segment≤63、exact round-trip與bounded visible-row packets，JSON輸出到`build/reports/terminal-scale-<types>.json`。 |
| `docs/superpowers/plans/` | Active implementation/TDD contracts。Core world repository現行實作契約見`2026-07-17-core-storage-repository.md`；shared terminal profile、exact recipe presentation、EMI-first renderer、output routing、station/Axe Energy與texture family主線見 `2026-07-14-terminal-platform-emi-recipe-axe.md`，其舊target popup已由GitHub #23的persistent Transform target sidebar/use-card契約取代；built-in adapter authority、未來custom mod recipe/station adapters與server-owned multi-step planner見`2026-07-14-modded-recipes-multistep-crafting.md`。 |
| `docs/superpowers/specs/` | Active design specs；Core out-of-chunk ownership與no-migration契約見`2026-07-17-core-storage-repository-design.md`；Bus versioned state、capability/security/transaction分期見`2026-07-17-bus-automation-contract.md`；terminal/EMI/station/Axe revision 見 `2026-07-14-terminal-platform-emi-recipe-axe-design.md`，true-void lab generator/座標/預載/hotbar-reset 契約見 `2026-07-13-void-gui-test-lab-design.md`。 |
| `PLAN.md` | 完整設計計劃書(里程碑、能量系統、GUI)。 |

## Module Boundaries

- 計算入口集中在 **Storage Core**；`StorageCoreBlockEntity`只持有repository attachment與runtime topology/cache，永久inventory/energy/machine/descriptor state由overworld `CoreStorageRepository`的`CoreStorageRecord`唯一擁有。Storage Unit只是空間,不自行計算。
- 網路狀態屬伺服器端;客戶端 GUI 一律靠封包同步,**不要**在客戶端保存儲存狀態。
- `ImportBusBlockEntity`與`ExportBusBlockEntity`的directional active front transfer在同一cooldown各自嘗試一個item stack與一個bounded typed resource，並同時發現generic addon與native fluid/FE/optional chemical endpoints；Import在設定side/null提供insert-only能力，Export只在directionless提供filter-gated extract-only能力。Generic typed capability拒絕Item kind，Export item view只有一格且最多正常stack。兩種Bus以schema 2 `BusConfiguration`持久化server-owned設定與typed exact filters；menu packet以container/position/revision/owner/operator/current-BE驗證後原子commit並invalidate capability。Stable cached wrappers保持物件身分但每次call重驗live BE、mode/side、automation、escrow與loaded Core path；no-Core時inert。Structured actor guard拒絕recursive same-network/inverse call；item與typed immutable plan都在commit及external execute callback後重驗Bus/config/Core/network/topology/path/endpoint；detached target拒絕反向reclaim的item差額與typed remainder都走exact recovery，detached Bus改產生owner-stripped recovery drop，並以per-Bus recovery UUID和highest-priority drop protection守住任何拆除。
- Runtime UI 語系檔 `en_us.json` / `zh_tw.json` 必須保持完全相同 key set；Patchouli localized content 仍依 Patchouli 自己的 locale tree 管理。
- 本 repo 是根工作區 `diy_minecraft_mods` 內的獨立 git repo,版控自成一套。
